<%= @header %>
<% @config.keys.sort.each do |section| -%>
[<%= section %>]
<%   @config[section].keys.sort.each do |var| -%>
<%= "#{' ' * Integer(@indent)}#{var} = #{@config[section][var]}" %>
<%   end
   end -%>
